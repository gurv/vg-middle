import com.bmuschko.gradle.docker.tasks.image.*

task buildImage(type: DockerBuildImage) {
    dependsOn build
    tag = 'gurv/grafana:latest'
    inputDir = file(".")
    buildArgs = [
            'GRAFANA_VERSION': rootProject.grafanaVersion,
    ]
}